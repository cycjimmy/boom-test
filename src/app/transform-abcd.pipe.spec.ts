import { TransformABCDPipe } from './transform-abcd.pipe';

describe('TransformABCDPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformABCDPipe();
    expect(pipe).toBeTruthy();
  });
});
